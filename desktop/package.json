{
  "name": "mail-index-desktop",
  "private": true,
  "version": "1.0.0",
  "description": "Thin Tauri v2 GUI shell that walks a non-technical user through mail-index onboarding.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
