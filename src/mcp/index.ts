#!/usr/bin/env node
/**
 * mail-index-mcp server entry point.
 *
 * M0.1 scaffold: logs a stub line and exits 0. The stdio MCP server (PLAN §12
 * tool surface) lands in M3.4.
 */

function main(): number {
  process.stdout.write('mail-index-mcp stub\n');
  return 0;
}

process.exit(main());
